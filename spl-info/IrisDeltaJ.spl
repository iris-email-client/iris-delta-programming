SPL IrisDeltaJ {

	Features = {Base}

	Deltas = {dBase, dBaseCommand, dBaseException, dBaseModel, dBasePersistence, dBaseMail}

	Constraints {
		// Put boolean expression representation of the feature model here. 
	}

	Partitions {
		// Put mapping between deltas and feature configurations here. 
		{dBase, dBaseCommand, dBaseException, dBaseModel, dBasePersistence, dBaseMail} when (Base);		
	}

	Products {
		Simple = {Base}; 
	}  
}  
 